# Azure Web App Deployment Configuration

# For Node.js apps on Azure App Service, the startup command is configured via Azure CLI
# The startup command will be set to: node src/app.js

# Environment variables will be set in Azure Portal or via deployment workflow:
# NODE_ENV=production
# PORT=8000 (or process.env.PORT from Azure)
# COSMOS_DB_ENDPOINT=<your-cosmos-endpoint>
# COSMOS_DB_KEY=<your-cosmos-key>
# JWT_SECRET=<your-jwt-secret>
# etc.
