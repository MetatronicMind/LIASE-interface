# Environment Configuration
NODE_ENV=production
PORT=3000

# Application Settings
APP_NAME=LIASE-Test-App
APP_VERSION=1.0.0
APP_DESCRIPTION=Simple Node.js boilerplate for Azure deployment testing

# Security Settings
CORS_ORIGIN=*
TRUST_PROXY=true

# Logging
LOG_LEVEL=info

# Database (if needed later)
# DATABASE_URL=your_database_connection_string

# External APIs (if needed later)
# API_BASE_URL=https://api.example.com
# API_KEY=your_api_key
