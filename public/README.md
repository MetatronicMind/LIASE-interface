# Public Assets

This directory contains static assets that are served directly by Next.js.

Files in this directory can be accessed from the root URL. For example, a file `public/icon.png` would be available at `/icon.png`.
